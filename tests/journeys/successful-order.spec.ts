// import { test, expect } from '@fixtures';
// import { AUTHENTICATED, createCheckoutMock } from '@data';
// import { createRandom } from '@utils';

// const SCOPE = 'E2E';

// maybe not needed?

/*
test.beforeEach(async ({ page }) => {
  await test.step('⬜ Go to page', async () => {
    await page.goto('/inventory.html');
  });
});

for (const persona of ACCESS_USERS) {
  test.describe(`${persona.role}`, () => {
    test.use({ storageState: persona.storageState });

    test.skip(`${SCOPE}: Full multi-item flow with cart modification`, async ({ page }) => {
      // const { catalogUI } = catalogLoc(page);

      const setup = {};

      await test.step('⬜ Arrange: prepare state', async () => {});

      await test.step('🟦 Action: perform interaction', async () => {});

      // await expect.soft(catalogUI.element, '🟧 Assert: verify outcome').toBeVisible();
    });
  });
}
  */
